#!/usr/bin/env python
from setuptools import setup, find_packages

__version__ = '0.1'

setup(
    name='lander',
    description='Landing page templater.',
    version=__version__,
    packages=find_packages(),
)
